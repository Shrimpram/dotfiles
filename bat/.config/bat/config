# Set the theme to base16 Stella (see themes directory)
--theme='base16-stella'

# Style to use (what bat should show)
--style="full"

# Use italic text on the terminal (not supported on all terminals)
--italic-text=always

# Never wrap text
--wrap=never
